# Quackathon26
Flo App
Control your money, don't let it control you

## Run with Docker

docker run -p 3000:3000 -e GROQ_API_KEY=PERSONAL_GROQ_KEY ghcr.io/shathamansour/flo-app:latest
Then open: http://localhost:3000
NOTE: i will not be sharing my API key hahah, you can get it for free via console.groq.com → API Keys → create a new one and just plug it in there!!
